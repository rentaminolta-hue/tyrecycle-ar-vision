# Tirecycle AR Vision AI — Knowledge Base

The app uses local knowledge plus live counter values. The supplied `best-3.onnx` model has two detection classes: `tire_car` and `tire_truck`.

Edit `knowledge.json` as a human-readable reference when expanding the knowledge set. Company-specific operational claims should be based on approved Ozcom/Tyrecycle documentation.
